SET(SHARED_LIB_SUFFXI "${CMAKE_SHARED_LIBRARY_SUFFIX}")

SET(LIT_META_FILE "${CMAKE_CURRENT_SOURCE_DIR}/lit.site.cfg.py.in")
SET(LT_TEST_SRC_DIR "${CMAKE_CURRENT_SOURCE_DIR}")

CONFIGURE_FILE("${LIT_META_FILE}"
  "${CMAKE_CURRENT_BINARY_DIR}/lit.site.cfg.py" @ONLY
)

ADD_CUSTOM_TARGET(check
    COMMAND lit "${CMAKE_CURRENT_BINARY_DIR}" -v
    DEPENDS MiniKintPass
)
